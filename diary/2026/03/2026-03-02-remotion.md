# 專案實作紀錄：remotion
* **📅 日期**：2026-03-02
* **🏷️ 標籤**：`#Project` `#Remotion` `#Colab` `#DevLog`

---

> 🎯 **本次進度摘要**
> 完成 `colab-showcase` 專案開發，並成功打通「本地提交 -> GitHub -> Google Colab 算繪 -> 自動上傳 Drive」的完整自動化流程。解決了 Colab 環境下的 CPU 核心數限制與路徑解析問題。

### 🛠️ 執行細節與變更
* **Git Commits**：
  - `fix(colab): register ColabShowcase correctly in root and fix notebook entry point`
  - `feat: add colab-showcase scene files and constants` (含強制加入遭忽略的 scenes 資料夾)
* **核心檔案異動**：
  - 📄 `projects/colab-showcase/src/index.tsx`：重構為導出型組件。
  - 📄 `src/Root.tsx`：註冊 `ColabShowcase` 組合。
  - 📄 `package.json`：修復 start/render 腳本路由。
  - 📄 `render_colab.ipynb`：修復 `ENTRY_FILE` 與並行數設定。
* **技術實作**：
  - 使用 `@remotion/transitions` 與 `framer-motion` 實現產品展示動畫。
  - 解決了 Colab `Error: concurrency is set higher than the amount of CPU cores available` 報錯。
  - 修正了 Remotion CLI 在特製進入點下無法正確載入 `registerRoot` 的問題。

### 🚨 問題與解法 (Troubleshooting)
> 🐛 **遇到困難**：Colab 算繪時報錯 `Available CPU cores: 2, value set: 8` 且找不到 `Scene1ColabLogo`。
> 💡 **解決方案**：
> 1. 在算繪指令增加 `--concurrency=2`。
> 2. 將 `ColabShowcase` 註冊回全局 `src/Root.tsx`，並讓 Notebook 走預設入口點 `src/index.ts`。
> 3. 使用 `git add -f` 強制加入被 `.gitignore` 誤擋的專案原始碼。

### ⏭️ 下一步計畫 (Next Steps)
- [ ] 觀察 Colab 算繪後上傳 Google Drive 的最終影片品質。
- [ ] 若畫質不夠細膩，調整算繪參數（如 `--scale`）。
- [ ] 整合語音語音 (TTS) 或背景音樂。
