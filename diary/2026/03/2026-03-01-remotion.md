# 專案實作紀錄：remotion
* **📅 日期**：2026-03-01
* **🏷️ 標籤**：`#Project` `#DevLog` `#Remotion` `#Animation`

---

> 🎯 **本次進度摘要**
> 成功整合重量級與羽量級動畫庫（Framer Motion, Anime.js v4），並修復 LottieTest 預覽時的非同步載入報錯。

### 🛠️ 執行細節與變更
* **核心檔案異動**：
  * 📄 `projects/animation-playground/src/components/FramerMotionTest.tsx`：[NEW] 整合 Framer Motion v12，利用 `useCurrentFrame()` 驅動彈簧卡片與軌道動畫。
  * 📄 `projects/animation-playground/src/components/AnimeJsTest.tsx`：[NEW] 整合 Anime.js v4，採 `seek()` 模式同步時間軸。
  * 📄 `projects/animation-playground/src/components/LottieTest.tsx`：[FIX] 修復 `src` 屬性失效問題，改用 `fetch` + `animationData` + `delayRender` 模式。
  * 📄 `projects/animation-playground/src/index.tsx`：新增場景至 `Series` 序列。
  * 📄 `src/Root.tsx`：更新影片總時長至 900 幀。
* **技術實作**：
  * **確定性動畫**: 所有第三方庫（GSAP, Anime.js, Framer Motion）均緊密耦合至 Remotion 的當前幀，確保渲覽結果 100% 穩定。
  * **API 填坑**: 識別並解決了 Anime.js v4 的 Breaking Changes（如移除默認匯出、回調函數簽名變更）。

### 🚨 問題與解法 (Troubleshooting)
> 🐛 **遇到困難**：Anime.js v4 報錯 `TypeError: anime is not a function` 且 TS 報錯無默認匯出。
> 💡 **解決方案**：經查 v4 改為具名匯出，應使用 `import { createTimeline, stagger } from 'animejs'`。
> 🐛 **遇到困難**：Lottie 預覽空白並報錯 `Property 'src' does not exist`。
> 💡 **解決方案**：棄用 `src={URL}`，改用 `delayRender` 非同步 `fetch` JSON 後傳入 `animationData`。

### ⏭️ 下一步計畫 (Next Steps)
- [ ] 輸出 `AnimationPlayground` 高品質測試片。
- [ ] 探索更多 TransitionSeries 轉場特效。
- [ ] 錄製一段專案導覽影片。
