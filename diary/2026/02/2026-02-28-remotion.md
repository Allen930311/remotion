# 專案實作紀錄：remotion
* **📅 日期**：2026-02-28
* **🏷️ 標籤**：`#Project` `#DevLog` `#Remotion`

---

> 🎯 **本次進度摘要**
> 完成 Remotion 各項技能展示動畫 `SkillsShowcase`，並成功修復跨專案引用導致的全域編譯失效問題。

### 🛠️ 執行細節與變更 (追加：Animation Playground)
* **核心檔案異動**：
  * 📄 `projects/animation-playground/src/index.tsx`：主進入點與場景序列管理。
  * 📄 `projects/animation-playground/src/components/D3Test.tsx`：數據驅動的條形圖。
  * 📄 `projects/animation-playground/src/components/GSAPTest.tsx`：手動 seek 同步的 GSAP 時間軸。
  * 📄 `projects/animation-playground/src/components/LottieTest.tsx`：Lottie 向量動畫整合。
  * 📄 `projects/animation-playground/src/components/CanvasTest.tsx`：Vanilla Canvas 2D 粒子系統。
* **技術實作**：
  * **性能鐵律**: 成功將 D3, GSAP (seek 模式), Canvas 全部綁定至 `useCurrentFrame()`。
  * **佈局優化**: 發現 Remotion 預覽中的全白問題常源於 CSS 坍塌，全面採用 `AbsoluteFill` 解決。

### 🚨 問題與解法 (追加：Bundling Crash)
> 🐛 **遇到困難**：安裝 `@react-three/fiber` 後，Bundler 報出 `undefined at Object.internalBundle` 錯誤，預覽死機全白。
> 💡 **解決方案**：經交叉比對判定為套件相容性衝突。決策先行移除 3D 相依性，回歸純 2D 動效開發環境以保證編譯穩定。

### ⏭️ 下一步計畫 (Next Steps)
- [ ] 輸出 `AnimationPlayground` 高品質 MP4。
- [ ] 視覺優化：提升範例動畫的美感（Color Palettes, Easing）。
- [ ] 進階測試：嘗試集成 Framer Motion 或 Anime.js 等更羽量級的動畫庫。
- [ ] 考慮加入更多 TransitionSeries 特效。

