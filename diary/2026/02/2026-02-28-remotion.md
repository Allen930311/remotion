# 專案實作紀錄：remotion
* **📅 日期**：2026-02-28
* **🏷️ 標籤**：`#Project` `#DevLog` `#Remotion`

---

> 🎯 **本次進度摘要**
> 完成 Remotion 各項技能展示動畫 `SkillsShowcase`，並成功修復跨專案引用導致的全域編譯失效問題。

### 🛠️ 執行細節與變更
* **核心檔案異動**：
  * 📄 `projects/remotion-skills-showcase/src/`：建立場景組件 (Scene1-5) 與主進入點。
  * 📄 `src/Root.tsx`：註冊 `SkillsShowcase` Composition 並清理重複 Import。
  * 📄 `projects/auto-skill-intro/src/index.tsx`：補上 `import React` 修復 Bundling 錯誤。
* **技術實作**：
  * **2.5D Showcase**: 使用 `rotateY` 與 `perspective` 實作手機容器。
  * **物理分層**: 結合 `spring()` 與 `Easing` 提供高質感的動態回饋。
  * **時間軸管理**: 使用 `Series` 與 `Sequence` 確保多場景切換的精確度。

### 🚨 問題與解法 (Troubleshooting)
> 🐛 **遇到困難**：側邊欄完全看不到 `SkillsShowcase` 組件，Studio 無法載入。
> 💡 **解決方案**：發現 sub-project (`auto-skill-intro`) 缺少 `import React` 導致全域 Webpack/Vite 報錯。需確保所有 `.tsx` 文件即便只使用 `React.FC` 也必須顯式引用 React。

### ⏭️ 下一步計畫 (Next Steps)
- [ ] 輸出高品質 MP4 檔案供用戶展示。
- [ ] 考慮加入更多 TransitionSeries 特效。
- [ ] 優化手機容器的投影 (Box Shadow) 質感。
